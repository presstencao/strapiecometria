{
  "collectionName": "components_metadata_metadata_sections",
  "info": {
    "displayName": "metadata_section",
    "icon": "database"
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "maxLength": 255,
      "minLength": 3,
      "regex": "^[a-zA-Z]+([0-9-]*[a-zA-Z]-)*$"
    },
    "background": {
      "type": "boolean",
      "default": false
    }
  }
}
